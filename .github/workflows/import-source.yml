name: Import Token Vendor source

on:
  push:
    paths:
      - token-vendor-source.zip

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Expand source and clean temporary files
        run: |
          unzip -q token-vendor-source.zip
          rm token-vendor-source.zip
          rm .github/workflows/import-source.yml
      - name: Commit project
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: complete token vendor challenge"
          git push
